import 'package:flutter/material.dart';

class Explor extends StatelessWidget {
  const Explor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
